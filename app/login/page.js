import Login from "@/components/login"
import { Section } from "lucide-react"

const LoginPage = () => {
  return (
    <section className="flex justify-center items-center h-screen bg-gradient-to-r bg-slate-100">
      <Login />
    </section>
  )
}

export default LoginPage